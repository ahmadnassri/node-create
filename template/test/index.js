/* global describe, it */

'use strict'

var ${name} = require('..')

require('should')

describe('${name}', function () {
  it('must have at least one test', function (done) {
    ${name}()

    true.should.be.true

    done()
  })
})
